#!/bin/bash

# Install node apps
function configure_node_apps() {
  npm install -g typescript # needed for nvim
}

# Installed application configurations
function configure_installed_apps() {
  configure_node_apps
}
